[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
# 기본 규칙(E4/E7/E9/F) + import 정렬. 대규모 재포맷은 하지 않는다.
extend-select = ["I"]

[tool.ruff.lint.isort]
known-first-party = ["nps_tracker"]

[tool.pytest.ini_options]
testpaths = ["tests"]
